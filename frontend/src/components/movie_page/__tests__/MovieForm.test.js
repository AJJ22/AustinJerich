import React from 'react'
import { render, screen } from '@testing-library/react'

// Simple smoke test - just check if test runner works
test('basic test works', () => {
  expect(true).toBe(true)
})
